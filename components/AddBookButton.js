import React from 'react'
import Button from '@material-ui/core/Button'

const AddBookButton = () => {
  return <Button size="large" color="primary" variant="contained" fullWidth>Add</Button>
}

export default AddBookButton
